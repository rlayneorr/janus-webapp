import { Component } from '@angular/core';

export class Location {
        addressId: number;
        street: String;
        city: String;
        state: String;
        zipcode: String;
        company: String;
        active: boolean;
}
