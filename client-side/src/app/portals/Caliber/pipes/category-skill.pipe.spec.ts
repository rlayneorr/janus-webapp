import { CategorySkillPipe } from './category-skill.pipe';

describe('CategorySkillPipe', () => {
  it('create an instance', () => {
    const pipe = new CategorySkillPipe();
    expect(pipe).toBeTruthy();
  });
});
