import { DisplayBatchByYearPipe } from './display-batch-by-year.pipe';

describe('DisplayBatchByYearPipe', () => {
  it('create an instance', () => {
    const pipe = new DisplayBatchByYearPipe();
    expect(pipe).toBeTruthy();
  });
});
